import AccountLayout from "@/components/account/AccountLayout";
import AddressCard from "@/components/account/AddressCard";

export default function AddressesPage() {
  return (
    <AccountLayout pageTitle="Addresses">
      <AddressCard />
    </AccountLayout>
  );
}
